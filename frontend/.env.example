# URL base de la API (backend FastAPI).
# Dejar vacío para desarrollo local — Vite proxy redirige /api y /v1 a localhost:8000.
# En producción setear la URL completa, ej: https://api.tudominio.com
VITE_API_BASE_URL=