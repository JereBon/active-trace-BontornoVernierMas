"""core/security.py — RESERVADO para C-03 (JWT + Argon2id + AES-256).

This module will implement:
  - JWT access token creation and verification (python-jose)
  - Argon2id password hashing and verification (argon2-cffi)
  - AES-256 encryption/decryption for PII at rest (ENCRYPTION_KEY)
  - Refresh token rotation logic

C-01 intentionally leaves this empty.
Do NOT add auth logic here until C-03.
"""
